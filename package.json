{
  "name": "from-cow-to-beef",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5180",
    "build": "vite build",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "vite": "8.2.2"
  }
}
